# planer_link migrations

`planer_link` owns the `codelinks_planer_link` database.

The current web sync schema is generated by Drizzle from
`src/lib/server/db/schema.ts` into `drizzle/`. New product migrations should stay
product-local and must not read or write the platform database.
